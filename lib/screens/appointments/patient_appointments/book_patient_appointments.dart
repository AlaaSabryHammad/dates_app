import 'package:flutter/material.dart';

class BookPatientAppointments extends StatelessWidget {
  const BookPatientAppointments({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
